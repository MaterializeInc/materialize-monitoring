[project]
name = "grafana-dashboards"
# These versioned with all other dashboards.
version = "0.10.0"
description = "Grafana Dashboards for Materialize"
readme = "README.md"
requires-python = ">=3.12"

classifiers = [
    "Development Status :: 1 - Planning",
    "Private :: Do Not Upload",
    "Topic :: System :: Monitoring",
]

dependencies = [
    "py-mzmon-lib",
    "grafana-foundation-sdk",
]
