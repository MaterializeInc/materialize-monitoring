{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "schedule:automergeDaily",
    "customManagers:helmChartYamlAppVersions",
    "customManagers:makefileVersions",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests",
    "mergeConfidence:all-badges"
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "labels": ["dependencies", "auto-format"],
  "rebaseWhen": "automerging",
  "reviewersFromCodeOwners": true,
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "gitIgnoredAuthors": [
    "49001892+materialize-bot@users.noreply.github.com",
    "41898282+github-actions[bot]@users.noreply.github.com"
  ],
  "minimumReleaseAge": "3 days",
  "abandonmentThreshold": "1 year",
  "vulnerabilityAlerts": {
    "addLabels": ["security"],
    "automerge": true,
    "assignees": ["team:security", "@Alphadelta14"]
  },
  "major": {
    "addLabels": ["major"],
    "automerge": false
  },
  "packageRules": [
    {
      "description": "Do not manage the legacy/ directory",
      "matchFileNames": ["legacy/**"],
      "enabled": false
    },
    {
      "description": "Do not pin digests for Docker image tags referenced in Helm charts",
      "matchManagers": ["helm-values"],
      "matchDatasources": ["docker"],
      "pinDigests": false
    },
    {
      "description": "Make `oci://` subcharts visible to Renovate. helmv3 extracts them with the `docker` datasource, and docker versioning cannot parse a semver range, so every OCI dependency was silently dropped as `unsupported/unversioned value ^x.y.z (versioning=docker)` while the https:// charts (helm datasource) updated normally. `helm` versioning understands the ranges. Charts sourced over https:// are unaffected — this rule only matches the docker datasource.",
      "matchManagers": ["helmv3"],
      "matchDatasources": ["docker"],
      "versioning": "helm"
    },
    {
      "description": "A Chart.yaml `version:` must stay a semver range, but the `docker:pinDigests` preset matches on datasource alone and would try to pin a digest onto the OCI subchart entries now that they resolve. The existing helm-values rule above does not cover them (different manager).",
      "matchManagers": ["helmv3"],
      "matchDatasources": ["docker"],
      "pinDigests": false
    },
    {
      "description": "Bump Chart.yaml subchart ranges (e.g. ^1.8.0 -> ^1.11.0) so in-range releases still open PRs; helmUpdateSubChartArchives then refreshes Chart.lock",
      "matchManagers": ["helmv3"],
      "rangeStrategy": "bump"
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchDepTypes": ["!devDependencies"],
      "matchCurrentVersion": "!/^0/",
      "automerge": true
    },
    {
      "groupName": "Dev Dependencies (non-major)",
      "matchDepTypes": ["devDependencies"],
      "matchCurrentVersion": "!/^0/",
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": false
    },
    {
      "groupName": "Go (Docs) Dependencies",
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": false
    },
    {
      "description": "Kubernetes puts its breaking changes in the minor, so for anything pinned to a k8s version v1.34 -> v1.36 is the risky bump and v1.34.8 -> v1.34.9 is the routine one. PVP versioning treats X.Y as the major version.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["kubectl", "**/kubectl", "kindest/node", "**/kindest/node"],
      "separateMajorMinor": true,
      "versioning": "pvp"
    },
    {
      "description": "Treat Kubernetes major versions (X.Y+1 PVP versioning) as requiring manual review before opening.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["kubectl", "**/kubectl", "kindest/node", "**/kindest/node"],
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true,
      "automerge": false
    },
    {
      "description": "Do not upgrade grafana-foundation-sdk to v10+. Only 0.0.x versions are valid",
      "matchPackageNames": ["grafana-foundation-sdk"],
      "maxMajorIncrement": 1
    },
    {
      "description": "helm-docs is pinned to a commit pseudo-version; do not flag it as abandoned",
      "matchPackageNames": ["github.com/norwoodj/helm-docs"],
      "abandonmentThreshold": null
    },
    {
      "description": "Hugo is pinned twice: as a `tool` in go.mod, which is what the Makefile builds with `go run -tags extended`, and as `hugo-version` in the docs workflow, which installs a prebuilt binary. Group them so both move in a single PR. A split bump lets CI and local builds render with different Hugo versions, and that divergence is what let a non-extended local build hide behind a stale resource cache until an upgrade invalidated it.",
      "groupName": "Hugo",
      "matchPackageNames": ["github.com/gohugoio/hugo", "gohugoio/hugo"]
    },
    {
      "description": "peaceiris/actions-hugo wants a bare `0.165.0`, but the GitHub releases are tagged `v0.165.0`. Strip the prefix so the workflow keeps the form the action accepts. Scoped to hugo so the `v`-prefixed annotations elsewhere (kind, alloy) are untouched.",
      "matchDatasources": ["github-releases"],
      "matchPackageNames": ["gohugoio/hugo"],
      "extractVersion": "^v(?<version>.+)$"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update Dockerfile versions",
      "managerFilePatterns": ["/.*Dockerfile$/", "/.*Containerfile$/"],
      "matchStrings": [
        "#\\s?renovate: datasource=(?<datasource>.*) packageName=(?<packageName>\\S*)(?: registryUrl=(?<registryUrl>\\S*))?( versioning=(?<versioning>.*?))?\\n.*(_TAG|_VERSION)\\s*\\??=?\\s*\"?(?<currentValue>.*)\"?"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver-coerced{{/if}}"
    },
    {
      "customType": "regex",
      "description": "Update renovate-annotated versions in GitHub workflows. The key pattern allows digits and hyphens so this covers `with:` inputs (`hugo-version:`) as well as env vars (`KIND_VERSION:`) — an unhyphenated class silently skipped every action input, which reads as 'nothing to update' rather than as a broken manager.",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "#\\s?renovate: datasource=(?<datasource>\\S+) packageName=(?<packageName>\\S+)(?: registryUrl=(?<registryUrl>\\S+))?(?: versioning=(?<versioning>\\S+))?\\s*\\n\\s*[A-Za-z_][A-Za-z0-9_-]*:\\s*[\"']?(?<currentValue>[^\"'\\s]+)[\"']?"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver-coerced{{/if}}"
    },
    {
      "customType": "regex",
      "description": "Update renovate-annotated image refs (tag plus optional digest) in the Makefile. The bundled customManagers:makefileVersions preset only matches a bare version in a *_VERSION variable, so it cannot maintain an image:tag@sha256 pair. Capturing currentDigest is what bumps both halves together — a tag-only bump would leave the digest pinning the old image and silently win.",
      "managerFilePatterns": ["/(^|/)Makefile$/"],
      "matchStrings": [
        "#\\s?renovate: datasource=(?<datasource>\\S+) packageName=(?<packageName>\\S+)(?: versioning=(?<versioning>\\S+))?\\s*\\n\\s*[A-Za-z0-9_]+\\s*\\??=\\s*\\S+?:(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver-coerced{{/if}}"
    }
  ]
}
