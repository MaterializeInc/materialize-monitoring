version: '3.8'

services:
  # Local Materialize instance for testing
  materialize:
    image: materialize/materialized:latest@sha256:f751a45fa28d27a40158e1504922a79d83d61315e3cb6519d96ce363d1e4a5f7
    container_name: materialize
    ports:
      - "6875:6875"
      - "6876:6876"
    environment:
      - MZ_SOFT_ASSERTIONS=1
    healthcheck:
      test: ["CMD", "curl", "-s", "http://localhost:6876/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - monitoring

  # SQL Exporter - Collects metrics from Materialize
  sql_exporter:
    image: ghcr.io/justwatchcom/sql_exporter:latest@sha256:b34a9f584391a0593f69b6a94a39767134167bd9c5fde07e3a3262d43d7d81c9
    container_name: sql_exporter
    ports:
      - "9399:9237"
    volumes:
      - ./sql_exporter/config.local.yml:/config.yml:ro
    environment:
      - CONFIG=/config.yml
    depends_on:
      materialize:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9237/metrics"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - monitoring

  # Prometheus - Time-series database for Grafana
  prometheus:
    image: prom/prometheus:latest@sha256:c6b27ea434f8389bfe233fbc7be381cf50587c286e871bc842008f5a1b1908a7
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    depends_on:
      sql_exporter:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - monitoring

  # Grafana - Dashboard visualization
  grafana:
    image: grafana/grafana:latest@sha256:121a7a9ece6dc10b969f1f96eed64b4f07dfac0d0b8abc070f7cb83bbde86f63
    container_name: grafana
    ports:
      - "3001:3000"
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
      - GF_INSTALL_PLUGINS=
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - monitoring

  # Datadog Agent - Collects metrics via OpenMetrics for Datadog
  datadog-agent:
    image: gcr.io/datadoghq/agent:latest@sha256:44bfe2c475d9467e78e73e4327148d7e3902da5a48cbb1b0f6b84b01283e89d5
    container_name: datadog-agent
    ports:
      - "8125:8125/udp"  # DogStatsD
      - "8126:8126/tcp"  # APM
    volumes:
      - ./datadog/datadog.yaml:/etc/datadog-agent/datadog.yaml:ro
      - ./datadog/conf.d:/etc/datadog-agent/conf.d:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /proc:/host/proc:ro
      - /sys/fs/cgroup:/host/sys/fs/cgroup:ro
    environment:
      - DD_API_KEY=${DD_API_KEY:-}
      - DD_SITE=${DD_SITE:-datadoghq.com}
      - DD_LOGS_ENABLED=${DD_LOGS_ENABLED:-false}
      - DD_PROCESS_AGENT_ENABLED=${DD_PROCESS_AGENT_ENABLED:-false}
      - DD_APM_ENABLED=${DD_APM_ENABLED:-false}
      - DD_HOSTNAME=materialize-monitoring-local
    depends_on:
      sql_exporter:
        condition: service_healthy
    networks:
      - monitoring

volumes:
  prometheus_data:
    driver: local
  grafana_data:
    driver: local

networks:
  monitoring:
    driver: bridge
